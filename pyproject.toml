[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.62.0",
    "requests>=2.34.2",
]

[project.scripts]
ekantipur-scraper = "ekantipur_scraper:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
